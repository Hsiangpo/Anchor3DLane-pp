[系统状态]：Anchor3DLane-pp is deployed and the official checkpoint baseline is reproduced on OpenLane validation, current stable F1 = 62.610; archived high-water non-stable PV-TSF F1 = 62.635; latest successful stable experiment is `ptef`; latest archived non-stable improvement is `pv_tsf`; latest failed experiment is `rtlc`; current running experiment is none; ApolloSim is not mounted yet.

# Anchor3DLane-pp Agent Notes

## Goal
- Minimum target: OpenLane-v1.2 Anchor3DLane++ F1 must exceed 65 before the goal can be marked complete.
- Minimum research standard: the final successful solution must contain at least 3-5 real innovation points, not just one-off tuning.
- Challenge target: reach F1 70 with at least 5-8 real innovation points.
- Baseline reproduction target: official Anchor3DLane++ camera-only ResNet-50 720x960 F1 = 62.4.

## Current Structure
- Remote path: `/root/autodl-tmp/Anchor3DLane-pp` on `anchor3dlane-seetacloud`.
- Active code branch: `anchor3dlane-pp`, tracking `hsiangpo/anchor3dlane-pp`.
- `configs/openlane/`: OpenLane training and evaluation configs.
- `configs_v2/openlane/anchor3dlane++_r50x2.py`: official camera-only baseline config for OpenLane-v1.2 F1 = 62.4.
- `mmseg/models/lane_detector/`: Anchor3DLane and Anchor3DLane++ model implementation.
- `mmseg/datasets/lane_datasets/`: dataset loaders and OpenLane evaluation integration.
- `tools/train.py` and `tools/test.py`: baseline training and evaluation entry points.
- `docs/实验记录.md`: record stable successful experiments plus archived non-stable high-score candidates.
- `docs/失败路线.md`: record failed or regressed ideas so later iterations avoid them.

## Current Progress
- SSH alias `anchor3dlane-seetacloud` works without password.
- The project is installed on the remote server with `python setup.py develop --no-deps`.
- Runtime uses the server base Python with `torch 2.8.0+cu128`, `torchvision 0.23.0+cu128`, and `mmcv 1.7.1`.
- Compatibility patches are present for NumPy 2.x aliases, missing `mmcv-full` focal loss extension, missing MSDA extension fallback, optional ONCE/spconv imports, and DCNv2 via `torchvision.ops.deform_conv2d`.
- Official weights are present remotely:
  - `pretrained/resnet50_v1c-2cccc1ad.pth`
  - `checkpoints/Openlane/openlane_anchor3dlane++_r50x2.pth`
- The official checkpoint loads into the current `Anchor3DLanePP` model.
- Official checkpoint validation evaluation completed:
  - PID file: `/root/autodl-tmp/Anchor3DLane-pp/logs/baseline_eval.pid`
  - Latest log: `/root/autodl-tmp/Anchor3DLane-pp/logs/baseline_eval_20260510_053432.log`
  - Command: `PORT=29601 bash tools/dist_test.sh configs_v2/openlane/anchor3dlane++_r50x2.py checkpoints/Openlane/openlane_anchor3dlane++_r50x2.pth 2 --show-dir output/openlane/baseline_eval`
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/baseline_eval/evaluation_result.json`
  - Metrics: F1 `0.6240448139487519`, recall `0.613268850798542`, precision `0.6352062468171787`, category accuracy `0.9337897428296154`.
- Current stable score:
  - Experiment: `strip_sample + vdcc + eac_dr + frt_eat + lpf_t + lof_oc + lef_dff + pgb_od + ptef` / Projection-Tube Evidence Field.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/ptef_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6260965689592046`, recall `0.6172847431754795`, precision `0.6351636181766219`, category accuracy `0.935150488985113`.
  - Improvement over previous stable `strip_sample + vdcc + eac_dr + frt_eat + lpf_t + lof_oc + lef_dff + pgb_od`: `+0.0001416310252484` F1, or `+0.014163` points on the 0-100 reporting scale.
  - Improvement over official checkpoint baseline: `+0.0020517550104527` F1, or `+0.205176` points on the 0-100 reporting scale.
- Archived high-water non-stable score:
  - Experiment: PV-TSF / Pseudo-Verified Temporal State Fusion.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/pv_tsf_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6263535889879278`, recall `0.6181478390504519`, precision `0.6347801281879852`, category accuracy `0.9349384677926014`.
  - Improvement over stable PTEF: `+0.0002570200287232` F1, or `+0.025702` points on the 0-100 reporting scale.
  - Decision: keep PV-TSF as an archived high-water candidate and record it in `docs/实验记录.md`, but do not replace stable PTEF because the gain is too small for the added temporal complexity and runtime. Active code/config remain rolled back; evidence artifacts stay under the remote `logs/` and `output/openlane/experiments/pv_tsf_r50x2*` paths.
- Latest failed architecture experiment:
  - Selected idea: RTLC / Road-Topology Lane Complex Decoder.
  - Rationale: repeated residual/loss/temporal-memory variants produced tiny gains. RTLC directly changes the proposal-generation paradigm by replacing the fixed 30 anchor initialization with 40 ordered road-topology lanes from a lane-complex decoder, while keeping the evaluator-compatible output contract.
  - Code status: failed code/config were rolled back from the active branch. The removed code lived under `mmseg/models/lane_detector/topology/`, with config `configs_v2/openlane/experiments/anchor3dlane++_r50x2_rtlc.py`; registration changes in `mmseg/models/lane_detector/__init__.py` and the 40-proposal dynamic-shape edit in `anchor_3dlane_pp.py` were removed.
  - Review status: xhigh code review returned `PASS WITH FIXES`; the main fixes were partial unfreezing of RTLC-used refinement/classification heads, explicit freezing of unused original anchor-init modules, and two lane-complex loss edge-case fixes.
  - Smoke status: remote 2-GPU smoke passed at `samples_per_gpu=1` and formal `samples_per_gpu=3`; batch-3 20-iter smoke logged about `19738MB`, finite loss/grad, `batch_rtlc_cross_violation=0.0000`, and no OOM/NaN/Traceback/DDP hard error.
  - Formal training and evaluation completed without OOM/NaN/Traceback/RuntimeError/Killed/no-space hard error. Training log is `logs/train_rtlc_20260514_055420.log`, final checkpoint is `output/openlane/experiments/rtlc_r50x2/iter_10000.pth`, eval log is `logs/eval_rtlc_20260514_064509.log`, and result file is `output/openlane/experiments/rtlc_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6060571600784425`, recall `0.589523014701162`, precision `0.6235455228166277`, category accuracy `0.9202583610567968`.
  - Decision: RTLC is a severe regression below stable PTEF F1 `0.6260965689592046` and even below the reproduced official checkpoint F1 `0.6240448139487519`. The hard replacement of the 30 learned anchor initialization with 40 ordered topology lanes produced valid training but destroyed proposal quality. Avoid repeating direct ordered-lane anchor replacement without a warm-start bridge, teacher-forced proposal alignment, or a protected fallback to the stable anchor distribution.
- Previous failed architecture experiment:
  - Selected idea: PV-TSF / Pseudo-Verified Temporal State Fusion.
  - Rationale: the user explicitly rejected micro-tuning and asked for bolder architecture changes. PV-TSF breaks the single-frame limitation by loading the real previous adjacent OpenLane image, predicting a pseudo-verified temporal motion state, warping previous-frame proposal features, and fusing that state into the final `get_proposals -> cls/reg/vis` path while keeping the public inference output contract compatible.
  - Code status: failed code/config were rolled back from the active branch. The removed files were `mmseg/datasets/lane_datasets/openlane_adjacent_temporal.py`, `mmseg/models/lane_detector/temporal/`, and `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pv_tsf.py`; registration changes in dataset/model init files and temporal batch fields were removed.
  - Review status: xhigh code review initially found GT leakage, motion-direction, and fp32-boundary risks; all were fixed and re-review cleared the experiment before formal training.
  - Validation status: local and remote `py_compile` and `git diff --check` passed; dataset sanity confirmed train-only temporal motion labels and no GT motion leakage at test time; PTEF checkpoint load had expected missing keys only for `temporal_state_fusion.*` plus the known non-blocking `lane_evidence_field.tube_offsets`; single-GPU backward and 2-GPU DDP smoke passed.
  - Formal training and evaluation completed without OOM/NaN/Traceback/RuntimeError/Killed/no-space hard error. Training log is `logs/train_pv_tsf_20260514_005023.log`, final checkpoint is `output/openlane/experiments/pv_tsf_r50x2/iter_10000.pth`, eval log is `logs/eval_pv_tsf_20260514_044355.log`, and result file is `output/openlane/experiments/pv_tsf_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6263535889879278`, recall `0.6181478390504519`, precision `0.6347801281879852`, category accuracy `0.9349384677926014`.
  - Decision: PV-TSF improves over stable PTEF by only `+0.0002570200287232` F1, or `+0.025702` points on the 0-100 scale. This is below the `+0.1` point useful-change threshold and far below the target F1 > 65, so PV-TSF is treated as invalid and documented in `docs/失败路线.md`. Avoid repeating single-previous-frame proposal feature warp plus small gated final fusion.
- Previous failed architecture experiment:
  - Selected idea: LaneSpawn-IQD + Graph Commit / Lane Query Spawning with Image-Conditioned Query Decoder and Topology Graph Commit.
  - Rationale: PTLM-LSC and PRVQ showed that teacher fields and near-all-retained set commit are still too weak. LaneSpawn-IQD directly breaks the fixed 30-proposal ceiling by adding 16 image-conditioned spawned lane candidates, then uses a topology graph commit module to select and rescore the final candidate set before NMS. The goal is real recall expansion with explicit false-positive suppression, not another loss-weight or threshold tweak.
  - Code status: failed code/config were rolled back from the active branch. The removed files were under `mmseg/models/lane_detector/proposal/`; the removed config was `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lane_spawn_iqd.py`.
  - Review status: xhigh code review found and the main agent fixed three risks before formal training: graph combined accept logits no longer pollute base PTEF accept/duplicate loss, spawn auxiliary supervision now uses one-to-one spawn/GT matching, and graph count target is aligned with `min_keep=3`. Re-review passed with no blocker/high/medium findings.
  - Validation status: local and remote `py_compile` and `git diff --check` passed. Remote config/model/checkpoint load passed with expected missing keys only for `lane_query_spawner.*` and `graph_commit.*`, plus non-blocking unexpected `lane_evidence_field.tube_offsets`. Forward-test sanity passed with 46 total candidates, graph-selected 16, post-NMS outputs shape-aligned, finite proposals, and separated `accept_logits` / `graph_accept_logits`.
  - Smoke status: remote 2-GPU smoke passed at `samples_per_gpu=3` for 1 iter and 20 iter with finite losses/grad, no OOM/NaN/Traceback/DDP hard error, and about `18978MB` logged memory. Batch search found `samples_per_gpu=48` stable for 1 iter and 20 iter with logged memory about `26774MB`; `samples_per_gpu=52` and `56` OOM during backward and must not be used.
  - Formal training status: stopped early by design on the remote server. Training log `logs/train_lane_spawn_iqd_20260513_224341.log` reached `Iter [1020/10000]` with `loss=0.4832`, `grad_norm=0.4645`, and no OOM/NaN/Traceback/RuntimeError/Killed/no-space hard error. The stopped work dir is `output/openlane/experiments/lane_spawn_iqd_r50x2`, with no saved final checkpoint and no evaluation result.
  - Decision: LaneSpawn-IQD failed before F1 evaluation because its core mechanism did not activate. From about `Iter [390/10000]` through `Iter [1020/10000]`, `batch_graph_selected=3.0000`, `batch_graph_count_pred=3.0000`, and `batch_graph_selected_spawn=0.0000`, while `batch_spawn_aux_pos` stayed around `4.3-4.6`. In plain terms, the spawned lanes learned auxiliary targets but the graph commit stage never selected any spawned candidate, so the new 16-candidate path could not affect final predictions. Avoid repeating candidate spawning where the commit head can collapse to `min_keep` and starve new candidates.
- Previous failed architecture experiment:
  - Selected idea: PTLM-LSC / Pseudo-Temporal Lane Memory with Lane-Set Commit.
  - Rationale: true multi-frame pixel fusion is theoretically attractive, but the current remote OpenLane-v1.3 mount has no `prev_data_release` or raw ego-pose files, and the old `Anchor3DLaneMF` path is a different 360x480 model family. PTLM-LSC therefore keeps the PTEF inference backbone, builds a fixed offline lane-memory teacher cache from same-segment previous lane labels, predicts a compact temporal BEV memory from current BEV evidence, and adds a lane-set commit head that directly affects final NMS/eval scoring. The main research point is not another proposal-token delta; it is set-level commit/duplicate suppression driven by temporal lane memory.
  - Code status: failed code/config were rolled back from the active branch. The removed code lived in `mmseg/models/lane_detector/ptlm_lsc/`, `mmseg/models/lane_detector/field/ptlm_lsc_field.py`, `mmseg/datasets/lane_datasets/openlane_ptlm_lsc.py`, `tools/ptlm_lsc/build_cache.py`, and config `configs_v2/openlane/experiments/anchor3dlane++_r50x2_ptlm_lsc.py`.
  - Validation status so far: local and remote `py_compile` passed; local and remote `git diff --check` passed; remote config/model/checkpoint load passed with expected missing keys only `ptlm_lsc_field.*`, non-blocking unexpected `lane_evidence_field.tube_offsets`, and trainable roots `accept_calib_head` plus `ptlm_lsc_field`. Code-review findings about lane_detector top-level file count, GT anchor off-by-one, and accept-logit gradient coupling were fixed; re-review passed with `CLEAR`.
  - Data/cache status: full remote cache generation completed with `136496 / 136496` `.npz` files under `data/OpenLane/lane3d_1000_v1.3/ptlm_lsc_cache` using about `542MB`; dataset sanity returned `ptlm_memory=(2,128,48)` and `ptlm_valid=(1,128,48)`, with valid previous-frame support on non-initial segment samples.
  - Smoke, training, and evaluation status: remote single-sample `train_step` and backward passed after review fixes with finite `ptlm_memory_loss`, `lsc_commit_loss`, `batch_ptlm_valid`, `batch_lsc_*`, `bad_grads=[]`, and finite gradients for `ptlm_lsc_field.*`. Remote 2-GPU smoke passed for 1 iter, 20 iter, and batch search through `samples_per_gpu=48`; batch 48 used about `30129 / 32607MB` and stayed finite with no OOM/NaN/Traceback/DDP hard error. Formal training completed on 2026-05-13 at `Iter [10000/10000]` with `loss=0.2896`, `grad_norm=0.0196`, and no runtime OOM/NaN/Traceback/Killed/RuntimeError/no-space hard error. Final checkpoint `output/openlane/experiments/ptlm_lsc_r50x2/iter_10000.pth` is 951445473 bytes and passed `torch.load(map_location="cpu")` validation with `meta.iter=9999`, `state_dict_keys=767`, `has_ptlm=True`, and `has_accept=True`. Final validation log is `logs/eval_ptlm_lsc_20260513_201729.log`, result file is `output/openlane/experiments/ptlm_lsc_r50x2_eval/evaluation_result.json`, and metrics are F1 `0.6261198521485261`, recall `0.618640017689899`, precision `0.633782774200434`, category accuracy `0.9349353595337772`.
  - Decision: PTLM-LSC improves over stable PTEF by only `+0.0000232831893215` F1, or `+0.002328` points on the 0-100 scale. This is below the useful-change threshold and far below the significant-change threshold, so PTLM-LSC is treated as invalid and documented in `docs/失败路线.md`. Avoid repeating same-segment previous lane memory with near-all-retained lane-set commit.
- Previous failed architecture experiment:
  - Selected idea: PRVQ / Privileged Road-Volume Query Decoder.
  - Rationale: prior proposal/loss/rerank changes and TSP-LDT produced only tiny gains. PRVQ changes the intermediate representation: it predicts a privileged metric road-volume field from OpenLane 3D lane labels, samples the field back into proposal tokens, then uses a lane-set decoder to apply proposal geometry, quality, and class deltas while keeping the public inference contract compatible with Anchor3DLane++.
  - Code status: failed code/config were rolled back from the active branch. The removed code lived under `mmseg/models/lane_detector/road_volume/`, with config `configs_v2/openlane/experiments/anchor3dlane++_r50x2_prvq.py` loading `output/openlane/experiments/ptef_r50x2/iter_10000.pth`.
  - Validation status: local and remote `py_compile` passed; local and remote `git diff --check` passed; remote config/model/checkpoint load passed with `samples_per_gpu=48`, `nonroad_missing=[]`, expected PRVQ-only missing keys, unexpected `lane_evidence_field.tube_offsets`, and trainable roots `accept_calib_head`, `lane_evidence_field`, and `road_volume_decoder`.
  - Smoke status: remote 2-GPU smoke tests passed for 1 iter, 20 iter, and batch search up to `samples_per_gpu=48`. Batch 48 20-iter smoke reached `Iter [20/20]`, `loss=0.3508`, `grad_norm=0.0402`, no OOM/NaN/Traceback/DDP hard error, and GPU memory about `30155 / 32607MB`; batch 56 OOMed and must not be used.
  - Formal training status: completed on 2026-05-13. Training log `logs/train_prvq_20260513_050510.log` reached `Iter [10000/10000]` with `loss=0.3072`, `grad_norm=0.1862`, and no OOM/NaN/Traceback/Killed/RuntimeError/no-space hard error. The shutdown-only NCCL `destroy_process_group()` warning is non-fatal.
  - Mid-training checkpoint status: `output/openlane/experiments/prvq_r50x2/iter_5000.pth` was saved at 916M and passed `torch.load(map_location="cpu")` validation with top-level keys `meta`, `optimizer`, and `state_dict`; `state_dict_keys=803`, `meta.iter=4999`.
  - Final checkpoint status: `output/openlane/experiments/prvq_r50x2/iter_10000.pth` was saved at 916M and passed `torch.load(map_location="cpu")` validation with top-level keys `meta`, `optimizer`, and `state_dict`; `state_dict_keys=803`, `meta.iter=9999`. `/root/autodl-tmp` still has about `14GB` free after checkpoint save.
  - Final validation result:
    - Evaluation log: `logs/eval_prvq_20260513_121744.log`.
    - Result file: `output/openlane/experiments/prvq_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6261372708087776`, recall `0.6175201329595629`, precision `0.6349983065048042`, category accuracy `0.935188179743303`.
  - Decision: PRVQ improves over stable PTEF by only `+0.0000407018500260` F1, or `+0.004070` points on the 0-100 reporting scale. This is below the `+0.1` point useful-change threshold and far below the `+0.2` point significant-change threshold, so PRVQ is treated as invalid and documented in `docs/失败路线.md`. Do not repeat PRVQ as only road-volume field supervision plus proposal-token delta decoding.
- Archived non-stable architecture experiment:
  - Selected idea: TSP-LDT / Temporal Self-Supervised Pseudo-LiDAR Distillation Teacher.
  - Rationale: repeated proposal/loss/rerank changes are still producing tiny gains, and the remote cache lacks trustworthy ego-pose for true aligned temporal fusion. TSP-LDT instead builds an offline BEV/SDF/z/quality teacher from same-segment neighbor lane sets with strict self-alignment, then trains the single-frame model to sample that teacher field into final proposal tokens while keeping inference single-frame.
  - Code status: preserved on archive branch `archive/tsp-ldt-f1-62.627`; the main stable branch should continue from PTEF unless this branch is intentionally revisited.
  - Review status: code review and re-review passed after fixing missing-teacher handling, metric naming, and BEV feature reuse.
  - Smoke status: remote 2-GPU smoke tests passed with finite loss/grad, `batch_tsp_valid=6144.0000`, `tsp_ldt_loss` visible, and no OOM/NaN/Traceback/DDP hard error. Conservative `samples_per_gpu=3` smoke used about `18977MB`; batch search then pushed the formal config to `samples_per_gpu=40`, and the 20-iter DDP smoke peaked at `29003 / 32607MB` on both RTX 5090 GPUs while staying stable.
  - Teacher-cache status: full training teacher cache completed at `data/OpenLane/lane3d_1000_v1.3/tsp_ldt_cache` with `136496 / 136496` `.npz` files on 2026-05-12 19:53 CST.
  - Formal training status: the first final checkpoint save failed because `/root/autodl-tmp` was full (`OSError: [Errno 28] No space left on device`). The corrupt checkpoint and completed smoke checkpoints were removed, then large checkpoints from already failed DAGS, PC-Aug, and MP-BFR experiments were removed while keeping logs/configs/results. Training was resumed from `iter_5000.pth`, regenerated a valid `iter_10000.pth`, and passed checkpoint load validation.
  - Final validation result:
    - Training log: `logs/train_tsp_ldt_20260512_195315.log`.
    - Evaluation log: `logs/eval_tsp_ldt_valid_20260513_033304.log`.
    - Result file: `output/openlane/experiments/tsp_ldt_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.626269758244777`, recall `0.6182833665018939`, precision `0.634465171024004`, category accuracy `0.9350582147477361`.
  - Decision: TSP-LDT is a real architecture-level innovation and produced a positive signal, but it only improves over PTEF by `+0.0001731892855724` F1 (`+0.017319` on the 0-100 scale). This is too small to treat as the stable mainline result for the F1 > 65 goal, so keep it archived and continue new major-innovation work from the PTEF stable branch.
- Previous failed architecture experiment:
  - Selected idea: SCD-CC / Shadow Candidate Distillation with Coverage-Constrained Commit.
  - Rationale: SCD-CC is an architecture-level candidate-generation and distillation experiment, not a tuning-only change. It internally expands each public proposal into multiple shadow candidates, trains those shadows against evaluator-style coverage signals, and commits only a tiny gated residual back into the original 30 public proposals so extra candidates never enter final NMS directly.
  - Training and validation completed on the remote server without OOM/NaN/Traceback:
    - Training log: `logs/train_scd_cc_20260512_163501.log`.
    - Final checkpoint: `output/openlane/experiments/scd_cc_r50x2/iter_10000.pth`.
    - Eval log: `logs/eval_scd_cc_bg_20260512_171716.log`.
    - Result file: `output/openlane/experiments/scd_cc_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6260829149193411`, recall `0.6172490780566791`, precision `0.6351732761700607`, category accuracy `0.9350982224648611`.
  - Decision: SCD-CC regressed slightly below current stable PTEF F1 `0.6260965689592046`; failed code/config must stay rolled back and the failure is documented in `docs/失败路线.md`. Avoid repeating internal shadow candidates with only tiny residual commit-gate tuning.
- Latest successful score-improvement experiment:
  - Selected idea: PTEF / Projection-Tube Evidence Field.
  - Rationale: PGB-OD shows metric BEV evidence can add a small recall-oriented gain, but a single center projection sample can still miss thin lane evidence when monocular geometry or calibration is slightly off. PTEF keeps the 30-proposal path, matcher, NMS, thresholds, PGB-OD, LEF-DFF, LOF-OC, and LPF-T unchanged, then samples a fixed 3D meter-scale evidence tube around each proposal point: lateral `x` offsets `[-0.5, -0.25, 0.25, 0.5]m` and vertical `z` offsets `[-0.3, 0.3]m` projected through the real camera matrix. This is a constrained physical uncertainty model, not free deformable attention.
  - Code: `mmseg/models/lane_detector/field/lane_evidence_field.py` adds optional `tube_enabled` sampling and `batch_tube_*` metrics; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_ptef.py`, loading `output/openlane/experiments/pgb_od_r50x2/iter_10000.pth`.
  - Validation before formal training: local and remote `py_compile` and `git diff --check` passed; code review subagents gave PASS/FINAL PASS; remote config/model/checkpoint load passed with missing keys only `lane_evidence_field.tube_sample_proj.*`, `unexpected=[]`; CUDA shape sanity produced tube logits `[2,7,30,20]`, masked invalid points correctly, and initial delta `0.0`; remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18977MB`, finite `batch_tube_*`, finite losses/grad, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_ptef_20260512_143223.log`.
    - Final checkpoint: `output/openlane/experiments/ptef_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_ptef_20260512_150754.log`.
    - Result file: `output/openlane/experiments/ptef_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6260965689592046`, recall `0.6172847431754795`, precision `0.6351636181766219`, category accuracy `0.935150488985113`.
  - Decision: PTEF improves over previous stable PGB-OD F1 `0.6259549379339562` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached. The gain is still small, so the next iteration should pivot to a larger architecture-level idea such as SCD-CC shadow candidates or MRB-SDF road-ribbon quality reranking.
- Previous successful score-improvement experiment:
  - Selected idea: PGB-OD / Privileged Geometry BEV-Ownership Distillation.
  - Rationale: LEF-DFF confirmed that dense projected lane evidence can help precision, but the gain is tiny and still single-image/single-view. PGB-OD extends the same field with a lightweight BEV teacher from OpenLane 3D GT: it predicts a low-resolution BEV lane evidence map from image features, supervises it with GT 3D lane occupancy, and samples that BEV evidence back into the existing final proposal tokens. It keeps the 30-proposal path, matcher, NMS, thresholds, LPF-T, LOF-OC, LEF-DFF, and accept calibration behavior unchanged.
  - Code: `mmseg/models/lane_detector/field/lane_evidence_field.py` now supports optional `bev_enabled` BEV evidence logits, BEV GT target generation, BEV token sampling, and `bev_evidence_loss`; `mmseg/models/lane_detector/anchor_3dlane_pp.py` only passes normalized proposal geometry into the existing field; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pgb_od.py`.
  - Checkpoint source: `output/openlane/experiments/lef_dff_r50x2/iter_10000.pth`.
  - Validation before formal training: local and remote `py_compile` and `git diff --check` passed; code review subagent cleared the initial fake-BEV high risk after BEV feature lift was changed to project metric BEV grid cells into the image feature map; remote config/model/checkpoint load passed with missing keys only `lane_evidence_field.bev_*`, `unexpected=[]`, and trainable roots only `accept_calib_head` and `lane_evidence_field`; remote synthetic sanity produced nonzero BEV target and finite `bev_evidence_loss`; remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18976MB`, nonzero `batch_bev_evidence_pos`, finite `bev_evidence_loss`, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_pgb_od_20260512_131752.log`.
    - Final checkpoint: `output/openlane/experiments/pgb_od_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_pgb_od_20260512_135125.log`.
    - Result file: `output/openlane/experiments/pgb_od_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6259549379339562`, recall `0.6167996975597926`, precision `0.6353860581431697`, category accuracy `0.9348642697211377`.
  - Decision: PGB-OD improves over previous stable LEF-DFF F1 `0.625799625360854` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Previous successful score-improvement experiment:
  - Selected idea: LEF-DFF / Dense Lane Evidence Field Gated Fusion.
  - Rationale: LOF-OC recovered recall through ownership supervision but still only produced a tiny gain. LEF-DFF adds dense 2D lane-evidence supervision from projected OpenLane 3D GT and samples that evidence at the existing 30 proposal lanes. It keeps the 30-proposal path, matcher, NMS, thresholds, LPF-T, LOF-OC, and accept calibration behavior unchanged.
  - Code: `mmseg/models/lane_detector/field/lane_evidence_field.py` adds `LaneEvidenceField`; `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional final-only `lane_evidence_field` and model-level `lane_evidence_loss`; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lef_dff.py`.
  - Checkpoint source: `output/openlane/experiments/lof_oc_r50x2/iter_10000.pth`.
  - Validation before formal training: local and remote `py_compile` and `git diff --check` passed; code review subagent PASS; remote config/model/checkpoint load passed with missing keys only `lane_evidence_field.*`, `unexpected=[]`, trainable prefixes only `accept_calib_head` and `lane_evidence_field`; real-sample target sanity produced nonzero dense target; remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18976MB`, finite `lane_evidence_loss`, nonzero `batch_lane_evidence_pos`, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_lef_dff_20260512_120823.log`.
    - Final checkpoint: `output/openlane/experiments/lef_dff_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_lef_dff_20260512_124029.log`.
    - Result file: `output/openlane/experiments/lef_dff_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.625799625360854`, recall `0.6161577254213834`, precision `0.6357480836143437`, category accuracy `0.9351688869948677`.
  - Decision: LEF-DFF improves over previous stable LOF-OC F1 `0.6257406098709484` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Previous successful score-improvement experiment:
  - Selected idea: LOF-OC / Lane Ownership Field with Ownership Contrast.
  - Rationale: LPF-T raised precision but reduced recall, and BRF-LPF showed same-slot score floors are too weak. LOF-OC keeps the 30-proposal inference path, matcher, NMS, and thresholds unchanged, then models lane-slot ownership relations at the final `[B, C=128, N=30, L=20]` token field. It uses evaluator-clean and FRT-clean ownership labels so clean duplicate candidates can support a lane-level MIL objective without turning near-GT ambiguous proposals into geometry positives.
  - Code: `mmseg/models/lane_detector/field/lane_ownership_field.py` adds `LaneOwnershipField`; `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional final-only `ownership_field`; `mmseg/models/losses/lane_loss_v2.py` adds `ownership_contrast_losses`, `ownership_mil_losses`, owner-clean negative removal, and `batch_owner_*` logging; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lof_oc.py`.
  - Checkpoint source: `output/openlane/experiments/lpf_t_r50x2/iter_10000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and `git diff --check` passed.
    - Code review subagent found no blocking/high/medium OOM, tensor-shape, DDP, checkpoint-load, freeze-mode, or failed-route issue.
    - Remote config/model/checkpoint load passed; expected missing keys are only `ownership_field.*`, `unexpected=[]`, and trainable parameters are limited to `ownership_field.*` and `accept_calib_head.*`.
    - Remote synthetic sanity passed: ownership field output shape `[2, 128, 30, 20]`, owner logits shape `[2, 30, 30]`, zero-init delta `0.0`, and finite ownership losses.
    - Remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18976MB`, nonzero owner metrics, finite losses and grad norm, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_lof_oc_20260512_105156.log`.
    - Final checkpoint: `output/openlane/experiments/lof_oc_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_lof_oc_20260512_112646.log`.
    - Result file: `output/openlane/experiments/lof_oc_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6257406098709484`, recall `0.6168139636073128`, precision `0.6349294264784706`, category accuracy `0.9351244296780283`.
  - Decision: LOF-OC improves over previous stable LPF-T F1 `0.6256777375241412` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Previous successful score-improvement experiment:
  - Selected idea: LPF-T / Lane-Point Field Transformer.
  - Rationale: repeated loss/ranking changes often disturb precision/recall. LPF-T instead changes the internal final-stage representation before flattening each proposal into a lane token: it applies longitudinal attention along the 20 y-points of each lane and lateral attention across the 30 lane slots at the same y-step. This preserves the 30-candidate inference path, NMS, thresholds, matcher, and FRT/EAC losses while giving the model a structured `lane x point` field before final classification/regression.
  - Code: `mmseg/models/lane_detector/field/lane_point_field.py` adds `LanePointFieldTransformer`; `mmseg/models/lane_detector/field/__init__.py` exports it; `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional final-only `lane_point_field`; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lpf_t.py`.
  - Checkpoint source: `output/openlane/experiments/frt_eat_r50x2/iter_8000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and `git diff --check` passed.
    - Remote config/model/checkpoint load passed; expected missing keys are only `lane_point_field.*`, `unexpected=[]`, and trainable parameters are limited to `lane_point_field.*` and `accept_calib_head.*`.
    - Remote standalone CUDA sanity passed: initial `out_proj` zero-init keeps `max_initial_delta=0.0`, output is finite, and `out_proj` receives gradients.
    - Code review subagent found and the main agent fixed one blocking loss-registration indentation issue before formal training; no remaining blocking OOM, DDP, tensor-shape, checkpoint-load, or freeze-mode issue remained.
    - Remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18976MB`, finite losses and grad norm, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_lpf_t_20260512_082504.log`.
    - Final checkpoint: `output/openlane/experiments/lpf_t_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_lpf_t_20260512_085209.log`.
    - Result file: `output/openlane/experiments/lpf_t_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6256777375241412`, recall `0.6125127502799712`, precision `0.6394210764360018`, category accuracy `0.9367344267200269`.
  - Decision: LPF-T improves over previous stable FRT-EAT F1 `0.6255466703380164` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Latest failed score-improvement experiment:
  - Selected idea: BRF-LPF / Bypass Recall Floor for Lane-Point Field.
  - Rationale: LPF-T improved precision but slightly reduced recall. BRF-LPF keeps the 30-proposal inference path, matcher, NMS, thresholds, and near-GT negative pool unchanged. It uses a no-grad teacher path from the pre-LPF final anchor features, then applies a conservative one-sided score/accept/rank floor only when both teacher and student are evaluator-clean and own the same GT lane.
  - Code was rolled back. The failed implementation temporarily added optional `recall_guard` and `predict_from_anchor_features()` to `mmseg/models/lane_detector/anchor_3dlane_pp.py`; it also added optional `recall_guard_losses`, owner-slot selection, score-floor loss, rank-floor loss, and `batch_recall_guard_*` logging to `mmseg/models/losses/lane_loss_v2.py`. The failed config was `configs_v2/openlane/experiments/anchor3dlane++_r50x2_brf_lpf.py`.
  - Checkpoint source: `output/openlane/experiments/frt_eat_r50x2/iter_8000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and target-file `git diff --check` passed.
    - Code review subagent found no blocking/high/medium OOM, tensor-shape, DDP, checkpoint-load, freeze-mode, or failed-route issue.
    - Remote config/model/checkpoint load passed; expected missing keys are only `lane_point_field.*`, and trainable parameters are limited to `lane_point_field.*` and `accept_calib_head.*`.
    - Remote synthetic sanity passed: the recall guard produces finite nonzero loss when teacher is stronger than student, and safely returns zero for empty targets or missing teacher tensors.
    - Remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `18976MB`, nonzero `batch_recall_guard_owner_pos`, finite losses and grad norm, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_brf_lpf_20260512_093248.log`.
    - Final checkpoint: `output/openlane/experiments/brf_lpf_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_brf_lpf_20260512_100542.log`.
    - Result file: `output/openlane/experiments/brf_lpf_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6256699349570143`, recall `0.6124200209710898`, precision `0.6395058602228332`, category accuracy `0.9365190648457145`.
  - Decision: BRF-LPF regressed very slightly below current stable LPF-T F1 `0.6256777375241412`; failed code/config are rolled back and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating pre-LPF no-grad teacher score-floor recall guard or only tuning `recall_guard_losses`, score/accept/rank margins, or top-k.
- Previous failed score-improvement experiment:
  - Selected idea: MP-BFR / Multi-Positive Boundary Fine Refinement.
  - Rationale: FRT-EAT improved calibration but the model still has very few Hungarian positives per image. MP-BFR keeps the 30-proposal inference path unchanged and adds final-only multi-positive boundary supervision: clean duplicate and near-GT repair proposals are removed from the classification negative pool and receive extra geometry/visibility supervision.
  - Code was rolled back. The failed implementation temporarily added optional `boundary_refine_losses`, boundary clean/repair proposal selection, `batch_boundary_clean`, `batch_boundary_repair`, and `batch_boundary_ignore` logging to `mmseg/models/losses/lane_loss_v2.py`; FRT dirty proposals were ignore-only and could not be selected for boundary supervision. The failed config was `configs_v2/openlane/experiments/anchor3dlane++_r50x2_mp_bfr.py`.
  - Checkpoint source: `output/openlane/experiments/frt_eat_r50x2/iter_8000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and `git diff --check` passed.
    - Remote config/model/checkpoint load passed with `missing=[]` and `unexpected=[]`.
    - Remote synthetic sanity passed: exact duplicate proposals are selected as clean, 1.8m near-GT proposals are selected as repair, far proposals remain negatives, and cross-neighbor FRT dirty proposals are ignore-only.
    - Code review subagent found no blocking OOM, DDP, tensor-shape, checkpoint-load, or project-constraint issue; the FRT dirty selection risk was fixed before smoke.
    - Remote 2-GPU DDP 1-iter and 20-iter smoke passed with memory about `27398MB`, nonzero `batch_boundary_repair`, finite losses and grad norm, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training completed:
    - PID file: `logs/train_mp_bfr.pid`.
    - Training log: `logs/train_mp_bfr_20260512_064029.log`.
    - Final checkpoint: `output/openlane/experiments/mp_bfr_r50x2/iter_10000.pth`.
    - Work dir: `output/openlane/experiments/mp_bfr_r50x2`.
    - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_mp_bfr.py`.
  - Formal validation evaluation completed:
    - PID file: `logs/eval_mp_bfr.pid`.
    - Evaluation log: `logs/eval_mp_bfr_20260512_074729.log`.
    - Result file: `output/openlane/experiments/mp_bfr_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.621979854494763`, recall `0.6121061679256453`, precision `0.6321773020348785`, category accuracy `0.9339583080951627`.
  - Decision: MP-BFR regressed below current stable F1 `0.6255466703380164` and official baseline F1 `0.6240448139487519`; failed code/config are rolled back and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating near-GT unmatched proposal promotion, boundary clean/repair supervision, or only tuning boundary thresholds/top-k/loss weights.
- Previous successful score-improvement experiment:
  - Selected idea: FRT-EAT / Frenet Road-Ribbon Topology with Evaluator-Aware Training.
  - Rationale: PC-Aug raised precision while collapsing recall, so this round protects EAC-DR's precision/recall balance instead of changing the image distribution. FRT-EAT keeps the 30-proposal geometry, matcher, NMS, and thresholds unchanged; it only changes the final evaluator-aware calibration target by using GT lane-local road ribbons to ignore or down-rank proposals that are evaluator-close but topologically crossing into a neighboring lane ribbon.
  - Code: `mmseg/models/losses/lane_loss_v2.py` adds optional `frt_enable`, GT-derived ribbon width, neighbor-gated ribbon violation logic inside `LaneLossV2.acceptance_labels`, and `batch_frt_dirty` logging; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_frt_eat.py`.
  - Checkpoint source: `output/openlane/experiments/eac_dr_r50x2/iter_10000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and `git diff --check` passed.
    - Remote config/model/checkpoint load passed; only six `accept_calib_head.*` parameters are trainable.
    - Remote synthetic sanity passed: exact-GT proposals are accepted, cross-neighbor evaluator-positive proposals become ignored FRT dirty samples, and single-lane cases are not penalized.
    - Code review subagent found no blocking OOM, DDP, tensor-shape, checkpoint-load, gradient, or failed-route issue.
    - Remote 2-GPU DDP smoke passed; memory about `18974MB`, `batch_frt_dirty` was nonzero, losses and grad norm stayed finite, and there was no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_frt_eat_20260512_053600.log`.
    - Final checkpoint: `output/openlane/experiments/frt_eat_r50x2/iter_8000.pth`.
    - Evaluation log: `logs/eval_frt_eat_20260512_055533.log`.
    - Result file: `output/openlane/experiments/frt_eat_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6255466703380164`, recall `0.6164715784668279`, precision `0.634892943535696`, category accuracy `0.9358638012838404`.
  - Decision: FRT-EAT improves over previous stable EAC-DR F1 `0.625498769106239` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Previous successful score-improvement experiment:
  - Selected idea: EAC-DR / Evaluator-Acceptance Calibrated Duplicate Reranker.
  - Rationale: recent geometry and candidate-generation routes mostly disturbed the current V-DCC precision/recall balance. EAC-DR keeps the 30-proposal geometry, matcher, and candidate count unchanged, freezes the stable V-DCC base, and trains only a final-layer evaluator-acceptance calibration head to rerank existing candidates with an OpenLane-style 1.5m / 75% acceptance label and duplicate ordering loss.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional final-only `accept_calib_head`, frozen-base train-mode handling, and weak calibrated NMS/output scores; `mmseg/models/losses/lane_loss_v2.py` adds optional `accept_calib_losses` and `dup_rank_losses`; `mmseg/apis/test_openlane.py` consumes optional calibrated scores; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_eac_dr.py`.
  - Checkpoint source: `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`.
  - Validation gates passed:
    - Local `py_compile` and `git diff --check` passed.
    - Remote `py_compile`, `git diff --check`, config/model/checkpoint load passed; expected missing keys are only `accept_calib_head.*`.
    - Remote train-mode sanity passed: only six `accept_calib_head.*` parameters are trainable; backbone, neck, and dynamic head stay in eval mode while the calibration head stays in train mode.
    - Remote synthetic acceptance-label sanity passed: exact-GT proposals become accepted, >1.5m shifted proposals become hard negatives, ignored proposals do not receive gradients.
    - Code review subagent found no remaining blocking or high-risk issue after fixing frozen-base eval mode and reducing `samples_per_gpu` back to 3.
    - Remote 2-GPU DDP smoke passed:
      - 1-iter smoke completed with no OOM/NaN/Traceback/DDP hard error.
      - 20-iter smoke completed; memory about `18974MB`, `batch_accept_pos`, `batch_accept_neg`, `batch_accept_ignore`, and `batch_dup_rank_pairs` were nonzero, losses stayed finite, and grad norm remained small.
  - Formal training and validation completed:
    - Training log: `logs/train_eac_dr_20260512_023028.log`.
    - Final checkpoint: `output/openlane/experiments/eac_dr_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_eac_dr_detached_20260512_025354.log`.
    - Result file: `output/openlane/experiments/eac_dr_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.625498769106239`, recall `0.6162575877540248`, precision `0.6350213248393889`, category accuracy `0.93595710615482`.
  - Decision: EAC-DR improves over the previous stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version. The minimum target F1 > 65 is still not reached.
- Latest failed score-improvement experiment:
  - Selected idea: PC-Aug / Projection-Consistent Camera Augmented Fine-tuning.
  - Rationale: previous structural attempts mostly disturbed precision/recall or added weak geometry priors. PC-Aug instead changes the training distribution while preserving exact 3D-to-image supervision: it applies small image-space affine perturbations before resize and left-multiplies `gt_project_matrix` / `gt_homography_matrix` by the same 2D transform, then fine-tunes the current EAC-DR model at low LR. This is a data-generalization route, not a new candidate, NMS, temporal, BEV, graph, topology, or threshold route.
  - Code: `mmseg/datasets/pipelines/lane_aug/projective.py` temporarily added `ProjectiveCameraAug`; `mmseg/datasets/pipelines/__init__.py` temporarily exported it; config was `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pcaug_eac.py`. Failed code and config are rolled back.
  - Checkpoint source: `output/openlane/experiments/eac_dr_r50x2/iter_10000.pth`.
  - Validation gates passed:
    - Local and remote `py_compile` and `git diff --check` passed.
    - Remote dataset sanity passed: for real OpenLane samples, augmented `gt_project_matrix` equals `H_aug @ original_project_matrix`, full pipeline returns `img=(3,720,960)`, `gt_project_matrix=(1,3,4)`, and no valid lane has fewer than two visible points after visibility update.
    - Remote config/model/checkpoint load passed with zero missing and unexpected keys; `freeze_base=False` is intentional, so PC-Aug is full-model low-LR fine-tuning rather than EAC-head-only training.
    - Code review subagent cleared the previous high-risk visibility-label issue after `update_visibility()` was added and low-visible lanes are invalidated.
    - Remote 2-GPU DDP smoke passed:
      - 1-iter smoke completed and wrote a checkpoint, with no OOM/NaN/Traceback/Killed/DDP hard error.
      - 20-iter smoke completed with memory about `27398MB`, finite losses, finite grad norm, and no OOM/NaN/Traceback/Killed/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_pcaug_eac_20260512_033447.log`.
    - Final checkpoint: `output/openlane/experiments/pcaug_eac_r50x2/iter_15000.pth`.
    - Evaluation log: `logs/eval_pcaug_eac_20260512_050000.log`.
    - Result file: `output/openlane/experiments/pcaug_eac_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.625321996136235`, recall `0.5778319887583545`, precision `0.6813170846878916`, category accuracy `0.9340488207591422`.
  - Decision: PC-Aug improved precision but collapsed recall and regressed below current stable EAC-DR F1 `0.625498769106239`; failed implementation code and config are rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating projection-consistent camera affine augmentation as a full-model fine-tuning route or only tuning `prob`, rotation/scale/translation/shear ranges, `min_visible_ratio`, learning rate, or iteration count.
  - Formal training setup:
    - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pcaug_eac.py`.
    - Work dir: `output/openlane/experiments/pcaug_eac_r50x2`.
    - `samples_per_gpu=3`, `lr=5e-6`, `max_iters=15000`, `checkpoint_config.max_keep_ckpts=1`.
    - Decision threshold: keep only if validation F1 exceeds current stable `0.625498769106239`.
- Previous failed score-improvement experiment:
  - Selected idea: DAGS / Depth-Aware Grade-State Anchor.
  - Rationale: previous pseudo-BEV, auxiliary depth, full-image road-surface, deformable sampling, temporal memory, and lane-set graph routes did not create evaluator-aligned 3D gains. DAGS keeps the 30-proposal path unchanged but injects a lane-local low-dimensional z/grade state directly into the anchor geometry before refinement.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `depth_grade_state` with low-dimensional z/grade deltas injected into anchor z slots before refinement; `mmseg/models/losses/lane_loss_v2.py` temporarily added optional `depth_state_losses`; config was `configs_v2/openlane/experiments/anchor3dlane++_r50x2_dags.py`. Failed code and config must stay rolled back.
  - Checkpoint source: `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`.
  - Validation gates passed: local `py_compile`, local `git diff --check`, code-review subagent PASS after fixing a DDP log Tensor-type issue, remote `py_compile`, remote `git diff --check`, remote config/model/checkpoint load, synthetic loss sanity, and 1-iter plus 20-iter 2-GPU DDP smoke.
  - Formal training and validation completed:
    - PID file: `logs/train_dags.pid`.
    - Training log: `logs/train_dags_20260512_003759.log`.
    - Final checkpoint: `output/openlane/experiments/dags_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_dags_20260512_013421.log`.
    - Result file: `output/openlane/experiments/dags_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6241304991935637`, recall `0.6165429087044288`, precision `0.6319071726177142`, category accuracy `0.9329762943436856`.
  - Decision: DAGS regressed below current stable F1 `0.6254260715979268`; failed implementation code and config are rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating lane-local low-dimensional z/grade delta injection into anchor z slots or only tuning `depth_state_losses`, `delta_min`, `delta_max`, `coeff_dim`, `gate_init`, or `depth_state_slope_weight`.
- Previous failed score-improvement experiment:
  - Selected idea: LSG-PG / Lane-Set Graph Prior Generator.
  - Rationale: previous failures show that output-stage hard topology losses, extra candidates, unaligned temporal memory, and extra image sampling freedom all destabilize precision/recall. LSG-PG moves lane-set reasoning earlier: after `expert_layer` predicts the initial 30 lane priors `(yaw, pitch, x_start)`, a latent graph generator treats the 30 slots as nodes in one lane-set graph and applies a zero-initialized soft graph residual to the initial priors before anchor generation.
  - Code: `mmseg/models/lane_detector/graph/lane_set_graph.py` temporarily added `LaneSetGraphPrior` with global image context, rank embeddings, pairwise prior bias, soft graph attention, small gated `delta_prior`, and logging stats. `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `lane_set_graph` config and logged `graph_gate`, `graph_prior_delta`, and `graph_edge_entropy`. Failed code has been rolled back.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lsg_pg.py`, loading `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, work dir `output/openlane/experiments/lsg_pg_r50x2`. Failed config has been removed.
  - Local validation so far:
    - `python -m py_compile mmseg/models/lane_detector/graph/lane_set_graph.py mmseg/models/lane_detector/anchor_3dlane_pp.py` passed.
    - `git diff --check` passed.
  - Validation before formal training:
    - Remote config/model/checkpoint load passed; expected missing keys are only `lane_set_graph.*`, with `unexpected=[]`.
    - Remote graph shape sanity passed: output prior shape `(2, 30, 3)`, initial max delta `0.0`, `graph_gate≈0.0180`, `graph_edge_entropy≈3.3623`.
    - Code review subagent found no blocking OOM, tensor-shape, DDP, checkpoint-load, gradient, default-behavior, or failed-route issue.
    - Remote 2-GPU DDP smoke passed with `samples_per_gpu=3`:
      - 1-iter smoke completed and wrote `output/openlane/smoke/lsg_pg_1iter/iter_1.pth`.
      - 20-iter smoke completed and wrote `output/openlane/smoke/lsg_pg_20iter/iter_20.pth`; memory about `27392MB`, `graph_gate=0.0180`, `graph_edge_entropy=3.3515`, finite loss/grad, and no OOM/NaN/Traceback/DDP hard error.
  - Formal training and validation completed:
    - PID file: `logs/train_lsg_pg.pid`.
    - Training log: `logs/train_lsg_pg_restart_20260511_225953.log`.
    - Final checkpoint: `output/openlane/experiments/lsg_pg_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_lsg_pg_20260511_235631.log`.
    - Result file: `output/openlane/experiments/lsg_pg_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6233816221598949`, recall `0.6146954555505625`, precision `0.6323167931714432`, category accuracy `0.9333910356637629`.
    - Note: the first formal attempt reached `iter 2490` but failed while saving `iter_2500.pth` because `/root/autodl-tmp` was full. Smoke checkpoints, LP-DCA failed training checkpoints, PLDD smoke checkpoint, the partial LSG-PG checkpoint, and the V-DCC intermediate `iter_7500.pth` were removed; stable `strip_sample` and `vdcc` final checkpoints and all evaluation results were preserved. Free space recovered to about `28GB`, then LSG-PG was restarted from the stable V-DCC checkpoint.
  - Decision: LSG-PG regressed below both current stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; failed implementation code and config are rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating initial-prior lane-set graph residuals or only tuning `hidden_channels`, `num_layers`, `delta_scale`, `gate_init`, edge temperature, or learning rate.
- Previous failed score-improvement experiment:
  - Selected idea: LP-DCA / Lane-Point Deformable Cross-Attention.
  - Rationale: current stable gains came from projection-aware local strip sampling plus V-DCC geometry supervision. LP-DCA upgrades fixed local strip sampling into proposal-guided multi-level deformable feature fusion, so each projected lane point can learn where to gather image evidence without adding proposals, changing matcher/NMS/thresholds, or using unavailable temporal pose.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `proposal_deform_fuse`, `proposal_deform_apply`, and a zero-initialized `ProposalDeformFeatureFusion` residual branch. Invalid projected points were masked so out-of-image locations did not add edge-clamped deformable residuals. Failed code has been rolled back.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_lpdca.py`, loading `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, work dir `output/openlane/experiments/lpdca_r50x2`. Failed config has been removed.
  - Validation before formal training:
    - Local `py_compile` and `git diff --check` passed.
    - Code review subagent reported no blocking OOM, tensor-shape, DDP, checkpoint-load, gradient, default-behavior, or failed-route issue. The main non-blocking precision risk was out-of-image reference point clamping; this was fixed with `valid_mask` residual masking and re-reviewed with no blocking issue.
    - Remote config/model/checkpoint load passed; expected missing keys are only `proposal_deform_fuser.*`, with `unexpected=[]` and initial gate about `0.0180`.
    - Remote 2-GPU DDP smoke passed with `samples_per_gpu=3`:
      - Pre-mask 1-iter and 20-iter smoke passed with memory about `27394MB`.
      - Masked 1-iter smoke passed with memory about `27395MB`, finite loss/grad, and no OOM/NaN/Traceback/DDP hard error.
      - Masked 20-iter smoke passed with memory about `27394MB`, finite loss/grad, and no OOM/NaN/Traceback/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_lpdca_20260511_210054.log`.
    - Final checkpoint: `output/openlane/experiments/lpdca_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_lpdca_20260511_215936.log`.
    - Result file: `output/openlane/experiments/lpdca_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.621898598348344`, recall `0.6132403187035016`, precision `0.6308048701648529`, category accuracy `0.9336345717579679`.
  - Decision: LP-DCA regressed below both current stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; failed implementation code and config are rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating standalone projection-point multi-level deformable cross-attention residual or only tuning `proposal_deform_heads`, `proposal_deform_points`, `proposal_deform_gate_init`, `proposal_deform_apply`, or learning rate.
- Previous failed score-improvement experiment:
  - Selected idea: PF-ATM / Pose-Free Anchor Token Memory.
  - Rationale: multiple geometry-only regularizers failed to move F1 enough, and true pose-based temporal fusion is blocked because the remote data lacks `prev_data_release` and raw per-frame poses. PF-ATM instead keeps the stable 30-proposal output path unchanged, samples the previous frame from the same segment as appearance-only context, and injects that context into the final anchor tokens through a zero-initialized temporal memory attention block. It adds temporal information without changing candidate count, matcher, NMS, or thresholds.
  - Code: `mmseg/datasets/lane_datasets/openlane_temporal.py` now supports `pose_free_neighbor=True` to sample previous images directly from the current segment without `prev_data_release`; `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional `temporal_memory`, multi-frame input splitting, detached previous-frame feature extraction, and final-only anchor-token cross-attention memory; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pfatm.py`.
  - Validation before formal training:
    - Local `py_compile` and `git diff --check` passed.
    - Remote dataset sanity passed: `OpenlaneMFDataset(pose_free_neighbor=True)` returns `img` shape `(3, 720, 960, 2)` and does not require `prev_poses`.
    - Remote config/model/checkpoint load passed; expected missing keys are only `temporal_gate_logit` and `temporal_attn.*`.
    - Remote 2-GPU DDP smoke passed:
      - `samples_per_gpu=2`, 1-iter and 20-iter smoke passed with memory about `24711MB`, `temporal_gate≈0.0180`, and no OOM/NaN/Traceback/DDP hard error.
      - `samples_per_gpu=3`, 1-iter and 20-iter smoke also passed with memory about `27413MB`, `temporal_gate≈0.0180`, and no OOM/NaN/Traceback/DDP hard error, so the formal run should use `samples_per_gpu=3`.
  - Formal training and validation completed:
    - Training log: `logs/train_pfatm_20260511_175519.log`.
    - Final checkpoint: `output/openlane/experiments/pfatm_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_pfatm_20260511_190422.log`.
    - Result file: `output/openlane/experiments/pfatm_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6246655423443693`, recall `0.6159437347085803`, precision `0.633637900643908`, category accuracy `0.9357944229784461`.
  - Decision: PF-ATM improved over the official baseline but still regressed below current stable F1 `0.6254260715979268`; failed implementation code and config must be rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating unaligned previous-frame anchor-token memory or only tuning `temporal_gate_init`, `prev_num`, or `prev_step`.
- Previous failed score-improvement experiment:
  - Selected idea: RSMC / Road-Surface Manifold Consistency.
  - Rationale: `ALTCR` showed that simple adjacent-lane gap/order regression is too weak and still slightly over-constrains the main branch. RSMC instead fits a lightweight road-surface manifold `z=f(x,y)` from matched GT visible points in each image, then only constrains matched positive `z_pred` to stay near that manifold. It does not add candidates, does not touch matcher/NMS/thresholds, and targets the low-frequency `z` geometry that V-DCC does not explicitly regularize.
  - Code: `mmseg/models/losses/lane_loss_v2.py` adds optional `road_surface_losses` with configurable basis/ridge/min-point gating; `mmseg/models/lane_detector/anchor_3dlane_pp.py` only forwards `batch_road_surface_points` into logs; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_rsmc.py`.
  - Validation before formal training:
    - Local `py_compile` and `git diff --check` passed.
    - Code review subagent found no blocking OOM/DDP/dimension/checkpoint issue; only residual risk is over-regularizing noisy road-height structure.
    - Remote config/model/checkpoint load passed.
    - Remote synthetic sanity passed: `road_surface_loss` is near zero when `pred=gt`, rises clearly when one lane's `z` is shifted, and returns zero safely for single-lane input.
    - Remote 2-GPU DDP smoke passed with `samples_per_gpu=3`:
      - 1-iter smoke work dir: `output/openlane/smoke/rsmc_1iter`, checkpoint `iter_1.pth`, memory `27397MB`, `batch_road_surface_points=35.3333`, no OOM/NaN/Traceback/DDP hard error.
      - 20-iter smoke work dir: `output/openlane/smoke/rsmc_20iter`, memory about `27397MB`, `batch_road_surface_points` stayed nonzero, `road_surface_losses_2_1` stayed finite, and no OOM/NaN/Traceback/DDP hard error occurred.
  - Formal training and validation completed:
    - Training log: `logs/train_rsmc_20260511_154124.log`.
    - Final checkpoint: `output/openlane/experiments/rsmc_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_rsmc_20260511_163834.log`.
    - Result file: `output/openlane/experiments/rsmc_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6245115219076193`, recall `0.6167925645360325`, precision `0.6324261282195098`, category accuracy `0.9345657249914631`.
  - Decision: RSMC improved over the official baseline but still regressed below current stable F1 `0.6254260715979268`; failed implementation code and config must be rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating direct full-image low-dimensional road-surface manifold fitting or only tuning its basis, ridge, min-point threshold, or fitting window.
- Previous failed score-improvement experiment:
  - Selected idea: AL-TCR / Adjacent-Lane Topology Consistent Regression.
  - Rationale: `requery_rank` proved that raising recall by adding extra output candidates destroys precision. AL-TCR keeps the stable 30-proposal output path unchanged and instead adds a matched-positive-only topology loss over adjacent GT lane pairs, constraining lateral gap, gap slope, and non-crossing order so lane-set structure stays coherent without touching matcher, NMS, thresholds, or candidate count.
  - Code: `mmseg/models/losses/lane_loss_v2.py` adds optional `topology_losses` with `topology_gap_weight`, `topology_slope_weight`, `topology_order_weight`, `topology_order_margin`, `topology_min_common`, width filtering, and reference-y sorting; `mmseg/models/lane_detector/anchor_3dlane_pp.py` only forwards `batch_topology_pairs` into logs; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_altcr.py`.
  - Validation before formal training:
    - Local `py_compile` and `git diff --check` passed.
    - Code review subagent found no blocking OOM/DDP/dimension/checkpoint issue; only residual risk is possible over-regularization, so the initial weight stays conservative at `topology_losses=0.02`.
    - Remote config/model/checkpoint load passed with no new model parameter mismatch risk because AL-TCR is a pure loss change.
    - Remote synthetic sanity passed: `topology_loss=0` when `pred=gt`, increases clearly when adjacent lanes are collapsed, and returns zero safely for single-lane input.
    - Remote 2-GPU DDP smoke passed with `samples_per_gpu=3`:
      - 1-iter smoke work dir: `output/openlane/smoke/altcr_1iter`, checkpoint `iter_1.pth`, memory `27397MB`, `batch_topology_pairs=3.0000`, `topology_losses_2_1=0.0002`, no OOM/NaN/Traceback/DDP hard error.
      - 20-iter smoke work dir: `output/openlane/smoke/altcr_20iter`, memory about `27395MB`, `batch_topology_pairs` stayed nonzero across iterations, `topology_losses_2_1` stayed finite, and no OOM/NaN/Traceback/DDP hard error occurred.
  - Formal training and validation completed:
    - Training log: `logs/train_altcr_20260511_141736.log`.
    - Final checkpoint: `output/openlane/experiments/altcr_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_altcr_20260511_151536.log`.
    - Result file: `output/openlane/experiments/altcr_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6246505348380557`, recall `0.6164573124193077`, precision `0.6330644801129005`, category accuracy `0.9348865601790619`.
  - Decision: AL-TCR improved over the official baseline but still regressed below current stable F1 `0.6254260715979268`; failed implementation code and config must be rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating direct adjacent-lane gap/order regression or only tuning topology weights, ordering margin, reference-y band, or width filters.
- Previous failed score-improvement experiment:
  - Selected idea: RQR-MPR / Recall-Reserved ReQuery with Multi-Positive Ranking.
  - Rationale: HPAS showed that simply adding more candidates turns into extra negative-sample pressure and hurts recall. RQR-MPR keeps the stable 30-slot main branch untouched, then selects `K=8` low-to-mid-confidence final proposals as recall-reserved seeds, runs a lightweight requery refine + rank branch on them, and trains that branch with multi-positive/ignore-near-GT ranking instead of pushing all added candidates into the main background pool.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional `requery_enable`, seed selection, detached requery token extraction, requery refine heads, and rank-aware test-time merge; `mmseg/models/losses/lane_loss_v2.py` adds `RecallRequeryLoss`; config is `configs_v2/openlane/experiments/anchor3dlane++_r50x2_requery_rank.py`.
  - Safety fix after code review: requery loss originally still backpropagated into shared `strip_weight_logits` and `position_encoder`; this was fixed by detaching the composed requery feature tensor before it enters the requery head, so the stable main branch stays isolated from the requery loss path.
  - Validation before formal training:
    - Local `py_compile` and `git diff --check` passed.
    - Remote config/model/checkpoint load passed; expected missing keys are only the new requery modules.
    - Code review subagent found no blocking OOM/DDP/dimension/checkpoint issue; one high-risk main-branch gradient leakage issue was reported and fixed before formal training.
    - Remote 2-GPU DDP smoke passed with `samples_per_gpu=3`:
      - 1-iter smoke work dir: `output/openlane/smoke/requery_rank_1iter`, checkpoint `iter_1.pth`, memory `27781MB`, requery losses visible in log, no OOM/NaN/Traceback/DDP hard error.
      - 20-iter smoke work dir: `output/openlane/smoke/requery_rank_20iter`, final memory `27781MB`, representative `batch_requery_positives` about `4-7`, `batch_requery_negatives` about `0-3`, no OOM/NaN/Traceback/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_requery_rank_20260511_124808.log`.
    - Final checkpoint: `output/openlane/experiments/requery_rank_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_requery_rank_20260511_134819.log`.
    - Result file: `output/openlane/experiments/requery_rank_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.5532158690914618`, recall `0.6530853894274322`, precision `0.4798390934412812`, category accuracy `0.8756303855775722`.
  - Decision: RQR-MPR caused a severe precision collapse and regressed far below both current stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; failed implementation code and config must be rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating direct requery-candidate merge into final NMS or only tuning `requery_num`, seed score band, positive/ignore distance, or ranking-loss weights.
- Previous failed score-improvement experiment:
  - Selected idea: HPAS-K15 / Hybrid Prototype Anchor Set.
  - Rationale: recent failed experiments often raised precision but reduced recall, suggesting the model is becoming too conservative rather than gaining candidate coverage. HPAS-K15 keeps the original 30 dynamic anchor slots and appends 15 data-driven fixed prototype anchors clustered from OpenLane training GT `(yaw, pitch, x_start)` priors, so the proposal set changes from 30 to 45 candidates without changing loss, matcher, NMS, inference threshold, V-DCC geometry loss, or the original dynamic slot checkpoint shapes.
  - Code: `mmseg/models/lane_detector/proposal/prototype_anchor_set.py` generates fixed prototype anchors; `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional `prototype_anchor_set`, `prototype_priors`, and `prototype_apply='init'`, appends the prototype anchors only at the first proposal generation step, and replaces downstream proposal-count assumptions with runtime `num_proposals`.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_hpas_k15.py`, loading `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, work dir `output/openlane/experiments/hpas_k15_r50x2`.
  - Prototype source: remote OpenLane training cache was scanned with robust filtering `|yaw| <= 0.25` and `|pitch| <= 0.08`; k-means produced 15 priors sorted by `x_start`, with shapes verified as prototype priors `[15, 3]`, anchors `[B,45,65]`, and proposals `[B,45,86]`.
  - Validation before formal training: local and remote `py_compile` and `git diff --check` passed; remote config/model/checkpoint load passed with only expected missing key `prototype_anchors.prototype_priors` and no unexpected keys; shape sanity passed; code review subagent found no blocking OOM, DDP, tensor-shape, checkpoint-load, default-behavior, or failed-route issue.
  - Remote 2-GPU DDP smoke passed:
    - 1-iter smoke wrote `output/openlane/smoke/hpas_k15_1iter/iter_1.pth` with no OOM/NaN/Traceback/DDP hard error; smoke checkpoints were deleted afterward.
    - 20-iter smoke log: `logs/smoke_hpas_k15_20iter_20260511_070635.log`.
    - Iter 10: memory `27395MB`, `batch_positives=4.7333`, `batch_negatives=40.2667`, loss `1.9280`, grad_norm `318.3642`.
    - Iter 20: memory `27395MB`, `batch_positives=3.5000`, `batch_negatives=41.5000`, loss `1.4382`, grad_norm `285.9323`.
  - Formal training and validation completed:
    - Training log: `logs/train_hpas_k15_20260511_070831.log`.
    - Final checkpoint: `output/openlane/experiments/hpas_k15_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_hpas_k15_20260511_080632.log`.
    - Result file: `output/openlane/experiments/hpas_k15_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6222420045455334`, recall `0.6087821788534378`, precision `0.6363104648931212`, category accuracy `0.9372627165064314`.
  - Decision: HPAS-K15 regressed below current stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; failed implementation code and config were rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating direct fixed GT-clustered prototype anchor appending or only tuning prototype count, clustering filters, or `prototype_apply`.
- Previous failed score-improvement experiment:
  - Selected idea: ABS-Gate / Anchor-BEV State Gate.
  - Rationale: PLDD showed that training-only depth supervision does not transfer enough 3D structure into proposal generation. ABS-Gate instead builds a lightweight BEV state from image features and samples that state along each anchor path, so the spatial prior participates directly in final-stage proposal/refinement without adding proposals, changing matcher behavior, changing inference thresholds, or adding local strip sampling freedom.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `anchor_bev_state`, BEV lift/projection helpers, anchor-path BEV sampling, and zero-initialized gated residual fusion into the anchor token. Failed code has been rolled back.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_abs_gate.py`, loading `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, final feature level/final refinement only. Failed config has been removed.
  - Validation before formal training: local and remote `py_compile` and `git diff --check` passed; remote config/model/checkpoint load passed with only expected BEV missing keys; remote shape sanity produced `bev_state=[2,64,96,48]` and `bev_anchor=[2,64,30,20]`; code review subagent found no blocking OOM, DDP, tensor-shape, checkpoint-load, default-behavior, or failed-route issue; 1-iter and 20-iter 2-GPU DDP smoke passed with finite loss/grad, memory about `27.4GB`, and no OOM/NaN/Traceback/DDP hard error.
  - Formal training and validation completed:
    - Training log: `logs/train_abs_gate_20260511_052931.log`.
    - Final checkpoint: `output/openlane/experiments/abs_gate_r50x2/iter_10000.pth`.
    - Evaluation log: `logs/eval_abs_gate_20260511_062600.log`.
    - Result file: `output/openlane/experiments/abs_gate_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6247938857930223`, recall `0.6103657101281804`, precision `0.6399206992880959`, category accuracy `0.9355821930576786`.
  - Decision: ABS-Gate improves over official baseline but regresses below current stable F1 `0.6254260715979268`; do not record it in `docs/实验记录.md`, keep only the failure analysis in `docs/失败路线.md`, and avoid repeating standalone single-frame anchor-BEV residual gating or only tuning BEV grid/z-bin/gate settings.
- Previous failed score-improvement experiment:
  - Selected idea: PLDD / Privileged Lane-Depth Distillation.
  - Rationale: ETAM was the strongest temporal architecture direction, but the current remote dataset mount does not include `prev_data_release`, and single-frame cache files do not contain enough ego-motion information to reconstruct trustworthy temporal pose alignment. To keep the new round in the “big innovation” regime without fabricating incorrect temporal geometry, this experiment pivots to training-time privileged depth supervision: use OpenLane's existing 3D lane annotations and camera matrices to generate low-resolution lane occupancy, inverse-depth, and height teacher targets, so the visual backbone can learn stronger 3D structure while inference remains pure monocular vision.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `priv_depth_distill`, a light `priv_depth_head`, online teacher-target generation, and `loss_priv_depth` that is only active during training. Failed code and config must stay rolled back.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_pldd.py`, loading `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, keeping `curve_geom_losses=0.05` and adding `priv_depth_loss_weight=0.03`; failed config must stay removed.
  - Validation before formal training: local `py_compile` and `git diff --check` passed; remote config/model/checkpoint load passed with only expected missing keys `priv_depth_head.weight` and `priv_depth_head.bias`; 1-iter and 20-iter 2-GPU DDP smoke passed with finite loss/grad, `loss_priv_depth` visible in logs, memory about `27.4GB`, and no OOM/NaN/Traceback/DDP error.
  - Formal training and validation completed:
    - Training log: `logs/train_pldd_20260511_022504.log`.
    - Evaluation log: `logs/eval_pldd_20260511_032101.log`.
    - Result file: `output/openlane/experiments/pldd_r50x2_eval/evaluation_result.json`.
    - Metrics: F1 `0.6246791574684367`, recall `0.613967887127032`, precision `0.6357708009682431`, category accuracy `0.9356250816703691`.
  - Decision: PLDD improves over official baseline but regresses below current stable F1 `0.6254260715979268`; do not record it in `docs/实验记录.md`, keep only the failure analysis in `docs/失败路线.md`, and avoid repeating standalone annotation-derived depth/height auxiliary supervision on the final image feature.
- Previous successful score-improvement experiment:
  - Selected idea: V-DCC / Visibility-weighted Differential Curve Consistency.
  - Rationale: `strip_sample` improved local image evidence but still regresses 3D lanes point-by-point. V-DCC adds a final-only, visibility-masked first/second differential curve consistency loss against GT x-curve geometry, using real y-step spacing. It does not add proposals, feature sampling freedom, matcher changes, confidence calibration, visibility BCE, or learned anchor priors.
  - Research: three `gpt-5.5 xhigh` subagents proposed V-DCC, GSPR, and PUME. V-DCC was selected first because it is a pure geometry-supervision signal with no new model parameters and the lowest risk of hurting precision through extra texture aggregation.
  - Code: `mmseg/models/losses/lane_loss_v2.py` adds optional `curve_geom_losses`, `curve_geom_axes`, `curve_slope_weight`, and `curve_curv_weight`. Default configs are unchanged unless `curve_geom_losses` is present in `loss_weights`.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_vdcc.py`, loading `output/openlane/experiments/strip_sample_r50x2/iter_10000.pth`, `samples_per_gpu=3`, `lr=1e-5`, `max_iters=10000`, final feature level/final refinement only.
  - Code review subagent found no blocking OOM, tensor-shape, DDP, checkpoint-load, gradient, default-behavior, or failed-route issue.
  - Local checks passed: `python -m py_compile mmseg/models/losses/lane_loss_v2.py` and `git diff --check`.
  - Remote config/model/checkpoint load passed with zero missing and unexpected keys.
  - Remote loss sanity passed on synthetic matched proposal/GT data with finite losses.
  - Remote 2-GPU DDP 1-iteration and 20-iteration smoke passed with finite loss/grad, no OOM/Traceback/NaN/DDP hard error, and memory about 27.4GB per GPU.
  - Formal 2-GPU training completed from `logs/train_vdcc_20260510_212223.log` and produced `output/openlane/experiments/vdcc_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_vdcc_20260510_221650.log`.
  - Result file: `output/openlane/experiments/vdcc_r50x2_eval/evaluation_result.json`.
  - Decision: improved over current stable `strip_sample` F1 `0.6252112086013109`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version.
- Completed score-improvement experiments:
  - `rcl` added final-only Refinement Contraction Loss on top of the stable `vdcc` checkpoint.
  - Formal 2-GPU training completed from `logs/train_rcl_20260511_002303.log` and produced `output/openlane/experiments/rcl_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_rcl_clean_20260511_012426.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/rcl_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6244708671489517`, recall `0.6112930032169938`, precision `0.6382294089016278`, category accuracy `0.9347680080595512`.
  - Decision: improved over official baseline but regressed below current stable F1 `0.6254260715979268`; failed implementation code and config must stay rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating standalone refinement contraction loss or only tuning `refine_contract_margin` / `refine_contract_losses`.
  - `vdcc_gds` added final-only Visibility-aware Geometric Decoy Suppression on top of the stable `vdcc` checkpoint.
  - Formal 2-GPU training completed from `logs/train_vdcc_gds_20260510_225752.log` and produced `output/openlane/experiments/vdcc_gds_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_vdcc_gds_20260510_235341.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/vdcc_gds_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6238212537003857`, recall `0.6146954555505625`, precision `0.6332220992897677`, category accuracy `0.93148480420266`.
  - Decision: regressed below current stable F1 `0.6254260715979268` and official baseline F1 `0.6240448139487519`; failed implementation code and config must stay rolled back, and the failure is documented in `docs/失败路线.md`.
  - Avoid repeating standalone near-GT geometric decoy suppression or only tuning `geo_decoy_tube`, `geo_decoy_topk_factor`, `geo_decoy_min_vis`, or `geo_decoy_losses`.
  - `far_semantic_fuse` added a zero-initialized far-range semantic residual from `anchor_feats[1]` only at `y >= 40m` on the final feature level and final refinement.
  - Formal 2-GPU training completed from `logs/train_far_semantic_fuse_20260510_195436.log` and produced `output/openlane/experiments/far_semantic_fuse_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_far_semantic_fuse_20260510_204903.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/far_semantic_fuse_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6230222048695299`, recall `0.6088392430435186`, precision `0.6378817135092613`, category accuracy `0.9329910283977163`.
  - Decision: regressed below current stable F1 `0.6252112086013109` and official baseline F1 `0.6240448139487519`; failed implementation code and config were rolled back, and the failure was documented in `docs/失败路线.md`.
  - Avoid repeating standalone far-range semantic residual fusion from `anchor_feats[1]` or only tuning `semantic_fuse_far_y`, `semantic_fuse_alpha_init`, or source feature level.
  - `curve_prior` added a zero-initialized learned quadratic 3D anchor prior on top of the stable `strip_sample` checkpoint.
  - Formal 2-GPU training completed from `logs/train_curve_prior_20260510_181104.log` and produced `output/openlane/experiments/curve_prior_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_curve_prior_20260510_190611.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/curve_prior_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6237906288117906`, recall `0.6097665361323319`, precision `0.6384749906315245`, category accuracy `0.9328194036196921`.
  - Decision: regressed below current stable F1 `0.6252112086013109` and official baseline F1 `0.6240448139487519`; failed implementation code and config were rolled back, and the failure was documented in `docs/失败路线.md`.
  - Avoid repeating learned quadratic anchor prior or only tuning `curve_prior_scale`, `curve_prior_mode`, or enabled layers.
  - `vis_bce` changed visibility supervision from SmoothL1 to BCE with positive weighting.
  - Formal 2-GPU training completed from `logs/train_vis_bce_retry_20260510_082812.log` and produced `output/openlane/experiments/vis_bce_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_vis_bce_20260510_091100.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/vis_bce_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6235742314274739`, recall `0.6226701761143566`, precision `0.624480915754884`, category accuracy `0.9352928519977174`.
  - Decision: regressed below the stable baseline F1 `0.6240448139487519`; failed experiment code was rolled back and the failure was documented in `docs/失败路线.md`.
  - Official prediction `test_conf` sweep from 0.30 to 0.60 also did not improve F1 and is documented in `docs/失败路线.md`.
  - `quality_calib` added quality-aware confidence calibration on the last feature level and final refinement only.
  - Formal 2-GPU training completed from `logs/train_quality_calib_resume_20260510_100110.log` and produced `output/openlane/experiments/quality_calib_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_quality_calib_20260510_103444.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/quality_calib_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6227141985407065`, recall `0.6076266290043012`, precision `0.6385701047829496`, category accuracy `0.9337999389775974`.
  - Decision: regressed below the stable baseline F1 `0.6240448139487519`; failed experiment code was rolled back and the failure was documented in `docs/失败路线.md`.
  - `eval_match` added evaluator-inspired clipped 3D distance cost to Hungarian assignment only.
  - Formal 2-GPU training completed from `logs/train_eval_match_20260510_110304.log` and produced `output/openlane/experiments/eval_match_r50x2/iter_10000.pth`.
  - Formal validation evaluation completed from `logs/eval_eval_match_20260510_114446.log`.
  - Result file: `/root/autodl-tmp/Anchor3DLane-pp/output/openlane/experiments/eval_match_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6206571803570284`, recall `0.6117780488326806`, precision `0.629797844865972`, category accuracy `0.9319983736531815`.
  - Decision: regressed below the stable baseline F1 `0.6240448139487519`; failed experiment code was rolled back and the failure was documented in `docs/失败路线.md`.
  - Avoid repeating standalone evaluator-distance Hungarian matching or only tuning `cost_eval` / `eval_dist_th`.
- Latest failed experiment verification notes:
  - Code review subagent found no blocking OOM, DDP, tensor-shape, or config issue.
  - Remote config load passed with `samples_per_gpu=2`, `max_iters=10000`, `cost_eval=0.5`, `cost_vis=0.0`, loading `checkpoints/Openlane/openlane_anchor3dlane++_r50x2.pth`.
  - Remote matcher sanity passed: default matcher output equals the previous cost formula; enabled eval cost returns finite `[30, 6]` matrices.
  - Remote 2-GPU DDP smoke passed for 1 and 20 iterations. The 20-iter run had finite losses and gradients, logged memory about 24.7GB, and no OOM/Traceback/NaN.
- Older failed score-improvement experiment:
  - Selected idea: geometry-normal strip residual fusion.
  - Rationale: `strip_sample` only samples horizontal image offsets. This experiment keeps the stable horizontal strip branch and adds a geometry-derived normal-direction strip branch along the projected lane curve, fused by one global scalar gate. It does not use content-based per-point offset weights.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `strip_normal_sample` and `strip_normal_alpha_logit`; failed code has been rolled back, preserving stable `strip_sample`.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_strip_normal.py`; failed config has been removed.
  - Research subagents proposed three options; this one was selected because it is closest to the successful `strip_sample` mechanism and avoids all recorded failed loss/matcher/threshold/content-attention routes.
  - Code review subagent found no blocking OOM, tensor-shape, DDP, checkpoint-load, or default-behavior issue. Two defensive fixes were added: fallback for `anchor_feat_len <= 1` and finite-value checking in normal-vector construction.
  - Remote config/model build passed: `samples_per_gpu=3`, `strip_normal_sample=True`, initial `alpha≈0.018`, loading `output/openlane/experiments/strip_sample_r50x2/iter_10000.pth`; only expected new missing key was `strip_normal_alpha_logit`.
  - Remote 2-GPU DDP 1-iteration and 20-iteration smoke passed with finite losses/gradients, memory about 27.4GB, and no OOM/Traceback/NaN/DDP hard error.
  - Formal 2-GPU training completed with PID file `logs/train_strip_normal.pid`, log `logs/train_strip_normal_20260510_164642.log`, and work dir `output/openlane/experiments/strip_normal_r50x2`.
  - Formal validation evaluation completed with PID file `logs/eval_strip_normal.pid`, log `logs/eval_strip_normal_20260510_174102.log`, and result path `output/openlane/experiments/strip_normal_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6238861006258989`, recall `0.6163788491579465`, precision `0.6315784775778349`, category accuracy `0.9327178367577269`.
  - Decision: regressed below current stable F1 `0.6252112086013109` and official baseline F1 `0.6240448139487519`; failed implementation code and config were rolled back, and the failure was documented in `docs/失败路线.md`.
  - Failure analysis: geometry-normal sampling raised recall but sharply reduced precision, likely because normal-direction residuals can aggregate adjacent-lane or road-edge texture under perspective and curve noise.
  - Avoid repeating standalone normal-direction strip sampling or only tuning `strip_normal_alpha_init`, normal offsets, or gate initialization.
  - Recent grad norms have intermittent finite spikes, including `893.2286` at `Iter [5560/10000]`, `761.4874` at `Iter [5780/10000]`, and `935.8562` at `Iter [6820/10000]`, but the latest checked sequence recovered to finite non-divergent values and loss did not show a sustained blow-up.
- Previous failed score-improvement experiment:
  - Selected idea: content-aware strip attention.
  - Rationale: stable `strip_sample` uses one global softmax weight over offsets for every lane point. It improved precision but reduced recall, so this experiment keeps the successful local strip evidence path and lets each anchor point adaptively weight left/center/right features.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` temporarily added optional `strip_agg='content'` with a zero-initialized `strip_score_layer`, so loading the stable `strip_sample` checkpoint started close to the previous global aggregation behavior.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_strip_content.py`.
  - Code review subagent found no blocking OOM, tensor-shape, DDP unused-parameter, checkpoint-load, gradient, or training-interruption issue.
  - Remote config/model build passed: `samples_per_gpu=3`, `strip_agg='content'`, `strip_score_layer.weight` shape `[1, 64]`, and zero initialization.
  - Remote checkpoint load passed from `output/openlane/experiments/strip_sample_r50x2/iter_10000.pth`; only expected missing keys were `strip_score_layer.weight` and `strip_score_layer.bias`.
  - Remote 2-GPU DDP 1-iteration and 20-iteration smoke passed with finite loss/grad, memory about 27.4GB, no OOM/Traceback/NaN/reduction error, and only a non-blocking `find_unused_parameters=True` performance warning.
  - Smoke outputs were cleaned; obsolete `strip_sample` intermediate checkpoints were removed, keeping `iter_10000.pth`.
  - Persistent OpenLane training image repair completed with `dirs=16`, `segments=798`, and `broken_symlinks=0`.
  - Randomly checked 36 training-list entries against both image and `.pkl` annotation paths; no missing files were found.
  - Formal 2-GPU training completed with PID file `logs/train_strip_content.pid`, log `logs/train_strip_content_20260510_151915.log`, `samples_per_gpu=3`, and work dir `output/openlane/experiments/strip_content_r50x2`.
  - Formal validation evaluation completed with PID file `logs/eval_strip_content.pid`, log `logs/eval_strip_content_20260510_161629.log`, and result path `output/openlane/experiments/strip_content_r50x2_eval/evaluation_result.json`.
  - Metrics: F1 `0.6235247637827419`, recall `0.6105797008409835`, precision `0.63703061970382`, category accuracy `0.9359335634972473`.
  - Decision: regressed below current stable F1 `0.6252112086013109` and official baseline F1 `0.6240448139487519`; failed implementation code and config were rolled back, and the failure was documented in `docs/失败路线.md`.
- Previous successful experiment:
  - Selected idea: projection-aware local lane-strip sampling.
  - Rationale: the original `Anchor3DLanePP` samples one image feature point per projected 3D anchor point. A small projection error can miss thin lane evidence, especially far away. This experiment samples three horizontal offsets around the projected point and learns a light softmax aggregation, while keeping loss, matcher, visibility supervision, and test thresholds unchanged.
  - Code: `mmseg/models/lane_detector/anchor_3dlane_pp.py` adds optional `strip_sample`, `strip_offsets`, `strip_center_bias`, and `strip_apply`.
  - Config: `configs_v2/openlane/experiments/anchor3dlane++_r50x2_strip_sample.py`.
  - Conservative setting: `strip_apply='final'`, so only the final refinement output uses strip sampling; default baseline configs keep `strip_sample=False`.
  - Local checks passed: `python -m py_compile mmseg/models/lane_detector/anchor_3dlane_pp.py`, `git diff --check`, and `rg` confirmed only one active `get_proposals` definition.
  - Code review subagent found no blocking OOM, DDP unused-parameter, tensor-shape, checkpoint-load, or gradient issue after the duplicate dead `get_proposals` definition was removed.
  - Remote config load and model build passed; `strip_weight_logits` starts with softmax weights about `[0.107, 0.787, 0.107]`.
  - Remote 2-GPU DDP smoke passed for 1 and 20 iterations at `samples_per_gpu=2`.
  - Batch-size probe passed at `samples_per_gpu=3`; 20-iter DDP smoke had no hard errors, finite loss/grad, and memory about 27.4GB per GPU, so the formal run should use `samples_per_gpu=3`.
  - Formal 2-GPU training completed with PID file `logs/train_strip_sample.pid`, log `logs/train_strip_sample_20260510_121835.log`, `samples_per_gpu=3`, and work dir `output/openlane/experiments/strip_sample_r50x2`.
  - Final checkpoint saved at `output/openlane/experiments/strip_sample_r50x2/iter_10000.pth`; `iter_5000.pth` and `iter_7500.pth` were kept by `max_keep_ckpts=2`; memory was about 27.4GB and no hard errors were found.
  - Formal validation evaluation completed with PID file `logs/eval_strip_sample.pid`, log `logs/eval_strip_sample_20260510_131335.log`, and result path `output/openlane/experiments/strip_sample_r50x2_eval/evaluation_result.json`.
  - Decision: improved over stable baseline F1 `0.6240448139487519`; keep the implementation, record it in `docs/实验记录.md`, and commit/push this stable version.
- Dataset status:
  - OpenLane source is reachable from the remote server through `hf-mirror.com`.
  - OpenLane validation list has 39981 entries and cache has 39981 `.pkl` files.
  - Validation images are under `data/OpenLane/images/validation`.
  - OpenLane raw training image archives are about 88.7G; raw validation image archives are about 22.3G.
  - `/root/autodl-tmp` is the only writable data mount and is now about 150G total. `/autodl-pub` is read-only and has no reusable OpenLane/ApolloSim dataset directory.
  - Persistent OpenLane training images are under `/root/autodl-tmp/openlane_train_images`; the old `/dev/shm` path is not reliable.
  - Preparation script `/root/autodl-tmp/jobs/prepare_openlane_train_parallel.sh` completed successfully and linked 798 training segment directories.
  - OpenLane training cache/list generation completed; latest list count is 138264 samples.
  - Current preparation scripts must check `images_training_N/segment-*` per archive before skipping extraction, otherwise later archives can be skipped incorrectly.
  - ApolloSim still needs a larger writable disk or an external mounted dataset path.

## Agent Workflow
- Main agent owns environment setup, training orchestration, scoring decisions, Git commits, and documentation.
- Use 2-3 `gpt-5.5 xhigh` subagents for research before each score-improvement experiment.
- Use one `gpt-5.5 xhigh` subagent for code review before starting training after code changes.
- When prompting subagents, explicitly tell them they are subagents and must not spawn or call further subagents.

## Rules
- Do not repeat environment deployment after the status line says deployment and baseline reproduction are complete.
- Baseline compatibility patches and documentation were committed after reproducing F1 = 62.4.
- Before each new experiment, read `docs/失败路线.md`.
- Commit and push only after F1 improves above the current stable score and is above baseline.
- If training fails or F1 regresses, revert to the last stable high-score commit and document the failure in `docs/失败路线.md`.
- Do not create new files with version suffixes such as `_v1` or `_v2`; record experiment versions in docs instead.
- Roll back failed implementation code promptly; keep only the stable code and documented lessons.
- Keep GPU utilization practical: avoid wastefully small settings after smoke tests, but do not exceed the known memory-safe envelope.
